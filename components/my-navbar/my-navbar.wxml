<!--components/my-navtab/my-navbar.wxml-->

<view class="nav-bar">
  <view 
    class="nav-item {{ (currentIndex === index) && 'active' }}" 
    wx:for="{{titles}}" 
    wx:key="index"
    bindtap="toggle"
    data-index="{{index}}"
  >
    <text>{{item}}</text>
  </view>
</view>
